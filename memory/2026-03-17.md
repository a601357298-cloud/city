# 2026-03-17

- 晚上排查飞书消息不回复问题：确认不是“已收到但未回复”，而是 **Feishu → OpenClaw 入站链路在 12:47 之后卡住**。日志中最后一条 Feishu 入站记录停在 12:47，此后用户再发消息未进入网关。
- 已采取修复：使用 gateway restart 重启 OpenClaw Gateway，强制重建 Feishu websocket 长连接。
- 修复后已主动向用户飞书发送测试消息，发送成功，messageId=`om_x100b54a1a5545ca4c2e64b148a7b6ff`，chatId=`oc_eb8eaefde1ccac4edb99f2de702106f0`。
- 后续处理原则：如果再次出现“飞书发来多条消息但完全无响应”，优先检查 `/tmp/openclaw/openclaw-YYYY-MM-DD.log` 中 `gateway/channels/feishu` 的最新入站时间；若长时间无新入站但 gateway 仍运行，则优先重启 gateway 恢复 Feishu 长连接。
- 相关异常线索：当天日志中出现过 `open.feishu.cn` DNS 解析失败（ENOTFOUND）以及网络接口变动引发的 unhandled promise rejection，可能与长连接失活有关。
