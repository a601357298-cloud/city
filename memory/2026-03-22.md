# 2026-03-22

- 用户要求记住本次外部 skill 安装与排障经验：
  - 外部 skill 需先做安全审查，再安装。
  - skill 查找采用多路径：ClawHub/SkillHub → awesome 索引仓库 → GitHub 官方/开源仓库。
  - 对未知 skill 要先看 `SKILL.md` / 仓库内容，重点排查越权读写、偷数据、乱联网、摸密钥等风险。
- 本次 Word 生成的有效解决方案：
  - 采用 Anthropic 官方 `docx` skill 思路。
  - 本地安装 `npm install -g docx` 后，Node 默认可能找不到全局模块；解决方法是先设置：`export NODE_PATH="$(npm root -g)"`。
  - 然后再运行 Node + `docx` 脚本生成真正有正文内容的 `.docx`。
- 用户要求把“每周学习内容整理为周报”沉淀成一个可复用 skill，并设置为每周日早上 9 点自动执行。
